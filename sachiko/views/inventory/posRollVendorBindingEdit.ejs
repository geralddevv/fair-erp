<% layout('/layout/boilerplate') -%>

<div class="main-content">
  <form
    class="needs-validation"
    action="/fairdesk/vendor-item/edit/pos/<%= binding._id %>"
    method="post"
    autocomplete="off"
    novalidate
  >
    <input type="hidden" name="returnTo" value="<%= returnTo || '' %>" />

    <div
      class="span-full form-subheading gap"
      style="display: flex; justify-content: space-between; align-items: center"
    >
      <h1 style="margin: 0">Edit Vendor POS Roll Binding</h1>
    </div>

    <div class="span-six">
      <label>POS Roll ID</label>
      <input
        type="text"
        class="form-control input-tag"
        value="<%= binding.posRollId?.posProductId || 'N/A' %>"
        readonly
      />
    </div>

    <div class="span-seven">
      <label>Vendor Name</label>
      <input
        type="text"
        class="form-control input-tag"
        value="<%= binding.vendorUserId?.vendorName || '' %>"
        readonly
      />
    </div>

    <div class="span-six">
      <label>Vendor User</label>
      <input type="text" class="form-control input-tag" value="<%= binding.vendorUserId?.userName || '' %>" readonly />
    </div>

    <div class="span-six">
      <label>Paper Code</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posPaperCode || '' %>" readonly />
    </div>

    <div class="span-seven">
      <label>Paper Type</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posPaperType || '' %>" readonly />
    </div>

    <div class="span-seven">
      <label>GSM</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posGsm || '' %>" readonly />
    </div>

    <div class="span-six">
      <label>Width</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posWidth || '' %>" readonly />
    </div>

    <div class="span-seven">
      <label>Meters</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posMtrs || '' %>" readonly />
    </div>

    <div class="span-six">
      <label>Core ID</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posCoreId || '' %>" readonly />
    </div>

    <div class="span-six">
      <label>Color</label>
      <input type="text" class="form-control input-tag" value="<%= binding.posRollId?.posColor || '' %>" readonly />
    </div>

    <div class="span-full form-subheading">
      <h1>Vendor Overrides</h1>
    </div>

    <div class="span-dual">
      <label>Vendor Paper Code</label>
      <input
        type="text"
        name="vendorPosPaperCode"
        class="form-control"
        value="<%= binding.vendorPosPaperCode %>"
        required
      />
    </div>

    <div class="span-dual">
      <label>Vendor Paper Type</label>
      <input
        type="text"
        name="vendorPosPaperType"
        class="form-control"
        value="<%= binding.vendorPosPaperType || '' %>"
        required
      />
    </div>

    <div class="span-dual">
      <label>Vendor GSM</label>
      <input type="number" name="vendorPosGsm" class="form-control" value="<%= binding.vendorPosGsm %>" required />
    </div>

    <div class="span-dual">
      <label>Minimum Order QTY</label>
      <input type="number" name="posMinQty" class="form-control" value="<%= binding.posMinQty %>" min="0" required />
    </div>

    <div class="span-dual">
      <label>Rate</label>
      <input type="number" name="posRatePerRoll" class="form-control" value="<%= binding.posRatePerRoll %>" step="0.01" min="0" />
    </div>

    <div class="span-full btn-grp" style="flex-direction: row-reverse">
      <button class="submit-btn" type="submit">Update Vendor POS Roll Binding</button>
      <button type="button" id="cancelBtn">Cancel</button>
    </div>
  </form>
</div>

<script>
  const form = document.querySelector("form");
  const submitBtn = form.querySelector('button[type="submit"]');
  const returnTo = <%- safeJson(returnTo || "") %>;

  document.getElementById("cancelBtn").addEventListener("click", () => {
    if (returnTo) {
      window.location.href = returnTo;
      return;
    }
    window.history.back();
  });

  form.addEventListener("submit", async (e) => {
    e.preventDefault();

    if (!form.checkValidity()) {
      form.reportValidity();
      return;
    }

    submitBtn.disabled = true;

    try {
      const res = await fetch(form.action, {
        method: "POST",
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(Object.fromEntries(new FormData(form))),
      });
      const data = await res.json();

      if (!res.ok || !data?.success) {
        submitBtn.disabled = false;
        if (window.showToast) window.showToast(data?.message || "Failed to update vendor binding", true);
        return;
      }

      window.location.href = data.redirect || returnTo || "/fairdesk/vendor/coordinator/view";
    } catch (err) {
      console.error(err);
      submitBtn.disabled = false;
      if (window.showToast) window.showToast("Failed to update vendor binding", true);
    }
  });
</script>
